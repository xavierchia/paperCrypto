platform :ios, '13.0'  # RxSwift 6.8.0 requires iOS 13.0 or later
use_frameworks!

target 'PaperCrypto' do
  pod 'RxSwift', '6.8.0'
  pod 'RxCocoa', '6.8.0'
  pod 'Alamofire'  # Add this since your networking code uses it
end